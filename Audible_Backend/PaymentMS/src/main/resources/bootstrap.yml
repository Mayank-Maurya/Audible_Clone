server:
  port: 8083
  
spring:
  application:
    name: purchaseMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        fail-fast: false
        enabled: true
        prefix: config
        defaultContext: common
        data-key: data
        profileSeparator: '::'
        format: YAML